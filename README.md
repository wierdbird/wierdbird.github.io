NDA is the one that was created roughly when the NDA was dropped in 2024, it hadnt been touched since in 2 years until "recently", 
canary is the branch that gets updated roughly close to when major updates are happening, like 30 minutes before. 
each selection of the top right branch changes the graph and the numbers according to which branch you choose.
the ATH for each branch is the largest jump in versions in a 24 hr period. for exp its current all time high is +36 version jumps in a day. current delta means the current jumps TODAY, 
so an example is if it says +9 in todays delta that means the version number for that specific branch jumped by 9, 
this doesnt take into account how many updates actually occured on that day it just counts the actual numerical jumps
